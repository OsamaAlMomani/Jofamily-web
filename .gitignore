# Repo-root ignores (this repo contains the app in /jofamily-web)

# Dependencies / build outputs
node_modules/
**/node_modules/
dist/
**/dist/

# Local env
.env
.env.*

# Firebase local cache
.firebase/

# Root-level accidental npm artifacts (app has its own package.json under /jofamily-web)
/package.json
/package-lock.json

# Local scratch folders
/jofamily-web/not now/
/jofamily-web/u generate one/
